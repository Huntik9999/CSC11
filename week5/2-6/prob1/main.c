#include "stdio.h"

int sum(int *a, int n);
void input (int *a, int n);

int main (){
    input (a, len);
    int total =sum (a , len);
    printf ("The sum is %d", total);

}

int sum (int a*, int n){
    int sum = 0;
    int i =0;
    while (i < n){
        sum =sum + a[i];
        i = i + 1;

    }
    return sum;
}

void input(int *a, int n){
    int i = 0;
    while 
}